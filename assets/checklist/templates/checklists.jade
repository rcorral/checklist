h1 Checklists

a.js-create-checklist(href='/checklists/create') Create a Checklist

each checklist in checklists
    article.checklist
        h2
            a.js-view-checklist(href='#{checklist.url}', data-id='#{checklist.id}')
                | #{checklist.title}
